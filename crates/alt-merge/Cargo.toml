[package]
name = "alt-merge"
description = "Line-level three-way (diff3) text merge with conflict markers"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
alt-diff.workspace = true
